# Build configuration consumed by Dockerfile and GitHub Actions.
# Keep commands compatible with /bin/sh in BUILD_IMAGE.
DOCKERFILE=Dockerfile
BUILD_IMAGE=node:20.11-slim
RUNTIME_IMAGE=node:20.11-slim
INSTALL_COMMAND='npm ci'
BUILD_COMMAND='npm run build'
RUNTIME_PREPARE_COMMAND='mkdir -p /runtime/.next && cp -a .next/standalone/. /runtime/ && cp -a .next/static /runtime/.next/static && cp -a public /runtime/public'
START_COMMAND='node server.js'
TEST_COMMAND='npm run format:check && npm run lint && npm run typecheck && npm test && npm run build'
